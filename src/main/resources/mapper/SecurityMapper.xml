<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jsg.house.repository.mybatis.OptionsMapper">

    <insert id="save">
        INSERT INTO SECURITY (HOUSE_ID, CCTV, VIEDO_PHONE, INTER_PHONE, FIRE_ALARM, FRONT_DOOR)
        VALUES(#{houseId}, #{cctv}, #{videoPhone}, #{interPhone}, #{fireAlarm}, #{frontDoor})
    </insert>

    <update id="update">
        UPDATE SECURITY
        SET CCTV = #{}, VIDEO_PHONE= #{}, INTER_PHONE = #{}, FIRE_ALARM = #{}, FRONT_DOOR = #{}
        WHERE SECURITY_ID= #{securityId}
    </update>

    <select id="findByHouseId">
        SELECT CCTV, VIEDO_PHONE, INTER_PHONE, FIRE_ALARM, FRONT_DOOR
        FROM SECURITY
        WHERE HOSUE_ID = #{houseId}
    </select>
</mapper>